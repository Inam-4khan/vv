# Client Environment Variables
VITE_API_URL=
# Development Auto-Login flag (Set to true only for local development, NEVER set in production)
VITE_DEV_AUTO_LOGIN=true

# SERVER ENVIRONMENT VARIABLES
# Note: GEMINI_API_KEY must ONLY be configured in the Vercel dashboard / server environment (never in client or VITE_ variables)
GEMINI_API_KEY=